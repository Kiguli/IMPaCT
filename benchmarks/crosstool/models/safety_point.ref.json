{
  "model": "safety_point.imdp",
  "description": "Point MDP, safety = P(never reach avoid={2}).",
  "provenance": "Exact: safe(1)=0.6, safe(0)=0.7*0.6+0.3*1=0.72. Point MDP, so PRISM Pmax=?[ !(F avoid) ] / Storm / IntervalMDP.jl all return exactly 0.72.",
  "checks": [
    {"prop": "safety", "label": "avoid", "state": 0, "pess": 0.72, "opt": 0.72, "tol": 1e-4}
  ]
}
